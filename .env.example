SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=rfp-monitor@example.com
SMTP_STARTTLS=true

# Inbox that the state portals send vendor notifications to (email-scan only).
# Gmail requires an app password here, not the account password.
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USERNAME=
IMAP_PASSWORD=
